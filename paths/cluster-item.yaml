get:
  tags:
    - Clusters
  description: Returns a cluster
  operationId: getCluster
  parameters:
    - name: "clusterId"
      in: "path"
      required: true
      schema:
        type: string
      example: "cluster1"
  responses:
    "200":
      $ref: "../responses/cluster-item.yaml"
    "400":
      $ref: "../responses/400.yaml"
